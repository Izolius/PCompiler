program aaa;
type
	enum=(one, two, three);
	limited=two .. three;
	myArray = array [one .. three] of integer;
	myArray2=myArray;
var
	arr: myArray;
	arr2:myArray2;
	a:limited;
procedure myproc(a: real; b: enum);
begin

end;
begin	
	myproc(1.0, a);
end.